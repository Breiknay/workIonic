import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-4-build-windows',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
